services:
  # Core Infrastructure
  redis:
    image: redis:7-alpine
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    networks:
      - orchestrator-net

volumes:
  redis_data:

networks:
  orchestrator-net:
    driver: bridge